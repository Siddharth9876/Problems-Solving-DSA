/**Q1. find the @minimum and @maximumelements in the array.**/

// sabse pehle kya input lega kya output dega
//1.  input kya lenge
//2. output kya hoga

/** 
 * my @firstApproach absolutely @WRONG because confused about the use of @Set keyword
 *  what does Set do?? 
 * @param {*} arr 
 * @returns  @array_with_unique_elements
 */
// poocha kya tha aur answer kya diya 


/**
 * 
 * @param {*} arr 
 * @returns 
 */

const minMax = (arr)=>{
  let newArr = [...arr];
  return newArr.sort();
}

// let arr = [3, 2, 1, 56, 10000, 167];

// console.log(minMax(arr));
// console.log(arr);
